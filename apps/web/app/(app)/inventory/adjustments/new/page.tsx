'use client';
import { ADJUSTMENTS_CONFIG, NewStockDocumentScreen } from '@/components/inventory/stock-documents';

export default function NewStockAdjustmentPage() {
  return <NewStockDocumentScreen cfg={ADJUSTMENTS_CONFIG} />;
}
