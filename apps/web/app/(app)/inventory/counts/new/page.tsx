'use client';
import { COUNTS_CONFIG, NewStockDocumentScreen } from '@/components/inventory/stock-documents';

export default function NewStockCountPage() {
  return <NewStockDocumentScreen cfg={COUNTS_CONFIG} />;
}
