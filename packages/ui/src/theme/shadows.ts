/**
 * Elevation. Hierarchy comes from background contrast and borders; shadows
 * are reserved for floating surfaces (dropdowns, popovers, dialogs).
 */
export const shadows = {
  sm: 'var(--elevation-sm)',
  md: 'var(--elevation-md)',
  lg: 'var(--elevation-lg)',
  overlay: 'var(--elevation-overlay)',
} as const;
